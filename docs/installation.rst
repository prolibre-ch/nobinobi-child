============
Installation
============

At the command line::

    $ easy_install nobinobi-child

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nobinobi-child
    $ pip install nobinobi-child
